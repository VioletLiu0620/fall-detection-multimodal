"""
STUB - not implemented.

Intended purpose: batch-run helper_functions.mov_to_csv() over a folder of
.mov/.mp4 clips and attach a Fall/ADL label to each resulting CSV based on
its parent folder name, so new raw video can be turned into labeled
training data without doing it one file at a time.

Currently unused by the rest of the pipeline (train.py, predict.py, and
data.py don't import this). Left in the repo as a placeholder for that
workflow rather than deleted, since it documents an intended next step.
"""
